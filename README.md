# Resume site

This repository contains the static source for Yourui Shao's personal website.

## Research pages

Research exports under `projects/` should reference repository attachments by relative URL rather than embedding binary data. Shared site assets use root-relative URLs. Web fonts are loaded from Google Fonts, while rendered MathJax output uses version-pinned font URLs.
